{
  "name": "quarto-voting",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint:check": "prettier '**/*.{json,yaml,md,css}' --check && turbo run lint:check",
    "lint:fix": "prettier '**/*.{json,yaml,md,css}' --write && turbo run lint:fix"
  },
  "devDependencies": {
    "prettier": "^3",
    "turbo": "^2"
  },
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
